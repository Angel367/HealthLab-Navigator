function Loading() {
    return (
        <span className={"loading"}>
                <span className="loading__spinner"/>
                Загрузка...
        </span>
    );
}

export default Loading;